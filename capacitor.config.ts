import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kotaro.pomodorotasks',
  appName: 'Pomodoro Tasks',
  webDir: 'dist'
};

export default config;
